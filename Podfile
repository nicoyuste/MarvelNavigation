# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Marvel-Navigator' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire'
  pod 'Hero'
  pod 'SwiftHash'
  pod 'PromiseKit'
  pod 'SDWebImage'
  pod 'SnapKit'

  # Pods for Marvel-Navigator

  target 'Marvel-NavigatorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Marvel-NavigatorUITests' do
    # Pods for testing
  end

end
